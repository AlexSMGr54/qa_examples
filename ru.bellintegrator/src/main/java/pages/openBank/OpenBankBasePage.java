package pages.openBank;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class OpenBankBasePage extends BasePage {

    public OpenBankBasePage(WebDriver driver) { super(driver); }

    /*
    Тут можно добавить элементы доступные с любой страницы сайта
    Например - меню, ссылки в футере и т.д.
     */
}
